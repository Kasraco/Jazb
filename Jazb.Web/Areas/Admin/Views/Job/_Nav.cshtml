<div class="navbar">
    <div class="navbar-inner">
        <a class="brand" href="#">مدیریت رشته شغلی</a>
        <ul class="nav">
            <li>
                @Ajax.ActionLink("رشته شغلی جدید", MVC.Admin.Job.ActionNames.Add, MVC.Admin.Job.Name, null,
                                         new AjaxOptions
                                         {
                                             HttpMethod = "Get",
                                             InsertionMode = InsertionMode.Replace,
                                             UpdateTargetId = "logOnModal",
                                             LoadingElementDuration = 0,
                                             LoadingElementId = "loadingMessage",
                                             OnSuccess = "LogOnForm.onSuccess"
                                         }, new { tabindex = "-1" })
            </li>

            <li class="pull-left">

                @Ajax.ActionLink("بازگشت", MVC.Admin.Job.ActionNames.Index, MVC.Admin.Job.Name, new AjaxOptions { HttpMethod = "GET", InsertionMode = InsertionMode.Replace, UpdateTargetId = "main-content", OnSuccess = "loadAjaxComponents", OnComplete = "hideLoading", OnBegin = "showLoading" })


            </li>
        </ul>
    </div>
</div>

